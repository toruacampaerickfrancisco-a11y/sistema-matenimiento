import React from 'react';

// Componente temporal para el NotificationCenter
// Este componente se puede expandir más adelante para mostrar notificaciones toast
const NotificationCenter: React.FC = () => {
  return (
    <div id="notification-center">
      {/* Las notificaciones toast aparecerán aquí */}
    </div>
  );
};

export default NotificationCenter;